<!-- res/drawable/message_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="8dp" />
    <solid android:color="@color/green" /> <!-- Background color for the message -->
    <stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- Border color for the message -->
</shape>